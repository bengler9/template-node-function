export const handler = async (event) => {
    // TODO implement
    const response = {
      statusCode: 200,
      body: JSON.stringify('Good Day from CloudBot!'),
    };
    return response;
  };
  